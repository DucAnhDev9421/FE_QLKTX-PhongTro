import React from 'react';
import { Layout } from '../../components/layout';

export default function Tenants() {
    return (
        <Layout>
            <h1 className="text-2xl font-bold mb-4">Quản lý người thuê (Table View)</h1>
        </Layout>
    );
}
